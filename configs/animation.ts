const baseSpringTranstion = {
  type: 'spring',
  stiffness: 32,
}

const baseEaseTranstion = {
  type: 'tween',
  ease: 'easeInOut',
}

const DELAY = 0.8

export { baseSpringTranstion, baseEaseTranstion, DELAY }
